<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Movie</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white min-h-screen">

  <div class="max-w-xl mx-auto mt-10 bg-gray-900 p-8 rounded-2xl shadow-xl">
    <h1 class="text-3xl font-bold text-yellow-400 mb-6 text-center">Edit Movie</h1>

    <form action="/movies/admin/<%= movie._id %>" method="POST" class="space-y-4">
      <input type="text" name="title" value="<%= movie.title %>" class="w-full px-4 py-2 rounded bg-gray-800 border border-gray-700 text-white" required>

      <textarea name="description" class="w-full px-4 py-2 rounded bg-gray-800 border border-gray-700 text-white" rows="4" required><%= movie.description %></textarea>

      <input type="text" name="genre" value="<%= movie.genre.join(', ') %>" class="w-full px-4 py-2 rounded bg-gray-800 border border-gray-700 text-white" required>

      <input type="text" name="language" value="<%= movie.language %>" class="w-full px-4 py-2 rounded bg-gray-800 border border-gray-700 text-white" required>

      <input type="url" name="posterUrl" value="<%= movie.posterUrl %>" class="w-full px-4 py-2 rounded bg-gray-800 border border-gray-700 text-white" required>

      <button type="submit" class="w-full bg-yellow-500 hover:bg-yellow-400 text-black py-2 rounded-xl font-bold transition">Update Movie</button>
    </form>
  </div>

</body>
</html>
