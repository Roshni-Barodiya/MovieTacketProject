<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= movie.title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white min-h-screen">

  <div class="max-w-4xl mx-auto mt-10 p-8 bg-gray-900 rounded-2xl shadow-xl">
    <div class="flex flex-col md:flex-row gap-8">
      <img src="<%= movie.posterUrl %>" alt="<%= movie.title %>" class="w-full md:w-1/3 rounded-xl shadow-lg object-cover">

      <div class="flex-1">
        <h1 class="text-4xl font-bold text-red-500 mb-4"><%= movie.title %></h1>
        <p class="text-sm text-gray-400 mb-2">Genres: <%= movie.genre.join(', ') %></p>
        <p class="text-sm text-gray-400 mb-4">Language: <%= movie.language %></p>
        <p class="text-lg text-gray-200 mb-6"><%= movie.description %></p>

        <a href="/login" class="inline-block bg-red-600 hover:bg-red-500 text-white font-semibold py-2 px-6 rounded-xl transition">Book Now</a>
      </div>
    </div>
  </div>

</body>
</html>
